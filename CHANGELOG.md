# 语音输入插件 · 完整变更记录（CHANGELOG）

> **项目**：DeepSeek Harness（DSH）语音输入插件（pluginId: `vmic-1`，静态部署版 `dsh-plugin-voice-input`）
> **覆盖范围**：v1 → v62（2026-08-16 ~ 2026-09-01）
> **本文件由三份来源合并而成**：
> 1. 《产品案例-语音输入模块优化记录.md》— v1→v8（PM 视角）
> 2. 历史会话档案（`~/.dsh/sessions/`，2026-08-23 考古解压）— v9→v16（原缺失段，已补全）
> 3. `voice-input-plugin/README.md` — v17→v28（工程视角）
>
> 版本号口径说明：v9 之前动态插件包名未带版本号，v9 起在包名中显式编号（如 `pkg-N` / `(Compact v10)` 等）；部署静态版后仍沿用同一版本序列。

## 侧记 SideNote（独立插件）

### 0.1.0 MVP · 2026-08-25

- 新增 `voice-evaluation-plugin`，与现有对话语音输入插件分离。
- 新增屏幕左右侧贴边悬浮入口、自动收起、全局快捷键 `Ctrl + Shift + Space`。
- 新增整段录音、复用现有 ASR、可编辑评价、类型/标签和最近记录。
- 新增 `voiceEvaluation` Host 服务，将评价原子写入 JSON/Markdown，并维护 `index.json`。
- 默认记录目录为 `.voice-evaluation-records`，删除记录先移入 `.trash`，保留恢复可能。
- 设置保存在 `dsh.voice.evaluation.v1`；Host 暂不可用时保留浏览器本地草稿。
- 首版暂不自动改动工作区外的 DSH 部署目录，待悬浮层和 Remote 路由验证后再部署。
- 新增 `side-note-app` 独立桌面版：不启动 DSH 也可直接录音、调用本地 `.voice-asr`、保存 JSON/Markdown；提供 `启动 SideNote.cmd` 与可传入位置的快捷方式生成脚本。

## 语音输入插件

### v62 · 2026-09-01

- Host 设置持久化迁移到稳定的 DSH 数据目录（支持 `DSH_VOICE_DATA_ROOT`），采用白名单、长度限制与临时文件 + rename 原子写入；保留 v59 工作区文件的一次性迁移路径。
- Client 保留 localStorage 作为离线缓存，但 Host 读写失败会在设置面板显示红字；清除 Key 同时覆盖 OpenAI、DeepSeek 与豆包凭据。
- 本地模型管理新增保存目录，Whisper/FunASR 缓存通过 Host 注入到该目录；本地 worker 在目录变更后自动释放并重建。
- 安装脚本改为默认只装 Python 依赖、不预下载大模型；`-WithModels` / `-ModelRoot` 用于显式预下载。`deploy.ps1` 只复制顶层 canonical 源文件，防止旧 `lib/` 快照回退。
- 设置 RPC 支持白名单字段的局部合并写入；FunASR 仅在完整 snapshots 缓存存在时标记为已下载。
- 移除运行代码中的个人绝对路径，并统一子进程失败返回，避免二次 `outcome` 异常。

---

## 快速总览

| 阶段 | 版本 | 主题 |
|---|---|---|
| 起步 | v1–v2 | 首发与修复（Web Speech API 实时转写） |
| 稳定性 | v3 | 错误可见化、静默自动续听 |
| 全浏览器 | v4 | WebAudio + 本地 faster-whisper 兜底 |
| 输入体验 | v5–v7 | 光标插入 / 光标回位 / 增量提交防覆盖 / 标点开关 |
| 模型化 | v8 | 模型接入 API + 设置选择界面 |
| 界面重构 | v9–v10 | 语音板块 → 收敛为 🎤+⚙ 双按钮 |
| AI 精修 | v11–v16 | 粗识别 + DeepSeek 精修链路（开关/配置/实时/折叠/持久化/上下文） |
| 可管理化 | v17–v22 | 网络重试/模型管理/服务商预设/豆包/引擎三项化 |
| 链路打通 | v23 | Typert 参数描述符修复（识别/精修真正生效） |
| 精修质量 | v24 | 延迟合并精修（伪句号掩码修复） |
| 界面打磨 | v25–v26 | 弹窗滚动 / 常驻滚动条 |
| 本地识别根治 | v27 | 常驻 worker / VAD 修正 / 会话代际 |
| 文案精简 | v28 | 设置弹窗说明精简 |

---

## 详细记录

### v1（2026-08-16）— 首发
- **目标**：最小可用闭环——输入栏加麦克风按钮，Chrome Web Speech API 实时转写写入输入框，AUTO 自动发送
- **关键问题**：误传空 Host 半区 → 启动失败
- **结果**：❌ 快速失败，暴露双半区规范（Client 半区 = UI/采音，Host 半区 = 服务）

### v2（2026-08-16）— 修复上线
- **改动**：改为纯 Client 半区
- **结果**：✅ 功能可用（Chrome/Edge 实时转写）

### v3（2026-08-16）— 稳定性
- **改动**：错误码可见化、15 秒静默自动续听、no-speech 重试、权限/网络错误明确提示
- **背景**：Chrome 对连续识别会话有 15 秒静默自动结束机制，网络抖动导致"说话中断报错"
- **结果**：✅ 中断可恢复、可诊断

### v4（2026-08-16）— 全浏览器
- **改动**：WebAudio 采音（16kHz PCM→WAV→base64）+ Host 端 faster-whisper 本地识别；`host.call` 私有 RPC；全程无临时文件
- **工程问题**：国内网络连不上 huggingface → 换 `hf-mirror.com`；Xet 协议镜像 401 → 禁用 Xet；Windows GBK 管道乱码 → 强制 UTF-8
- **结果**：✅ Firefox/Safari 可用，录音不出本机（隐私友好）

### v5（2026-08-16）— 光标位置插入
- **改动**：`selectionchange` 跟踪光标 + 插入点拼接 + CJK 智能空格
- **结果**：✅ 语音文本插入光标处而非末尾，与打字一致

### v6（2026-08-16）— 光标回位
- **改动**：识别完焦点归还输入框 + 双 rAF 时序 + 非打扰保护
- **结果**：✅ 识别完光标停留在插入文本末尾，可继续打字

### v7（2026-08-16）— 数据"召回"修复 + 标点开关
- **改动**：增量提交（只追加新 final，不重建整段回填，用户编辑不再被覆盖）；标点剥离开关
- **结果**：✅ 编辑不被覆盖；标点可控

### v8（2026-08-16）— 模型接入 API + 选择界面 + 质量优化
- **改动**：Host 端后端抽象（`voice.listBackends` / `voice.transcribe` 双 RPC）：本地 faster-whisper（tiny~large-v3）与 OpenAI 兼容云 API 可插拔；设置页选择引擎/模型/语言/质量档；本地引擎启用标点引导（`initial_prompt` + `condition_on_previous_text=True`）
- **验证**：同一段合成语音 v4 无标点 → v8 带逗号与句号
- **结果**：✅ 可接更强模型；实测标点输出改善

### v9（2026-08-17）— 界面改版：语音板块（Voice Panel）
- **改动**：语音功能整合为输入框上方独立「语音板块」——左 🎤 麦克风，右侧 chip（实时/本地·model）+ 标点 + AUTO + ⚙ 设置；设置分层展开；实时路径保留为自动默认
- **配套**：首次创建 `voice-input-plugin/README.md` 恢复说明 + `client.js`/`host.js` 源码快照落盘
- **结果**：✅ 板块上线（`pkg-1`）

### v10（2026-08-17）— UI 收敛：紧凑双按钮（Compact）
- **需求**：麦克风功能前端太大，缩小放到模型选择旁边
- **改动**：输入栏右侧（模型选择旁）仅 🎤+⚙ 两个小按钮；引擎/模型/语言/质量/标点/AUTO 全部收进 ⚙ 浮层设置；标点/AUTO 状态提升至共享 prefs
- **结果**：✅ 紧凑版上线（`pkg-3`，run-2）

### v11（2026-08-17）— AI 精修（豆包式两步方案）
- **需求**：本地引擎识别效果一般，希望用"粗识别 + AI"方式提升文本质量，AI 用 DeepSeek
- **改动**：接入豆包式两步流水线：粗识别（本地/云端 ASR）先上屏 → DeepSeek（chat completions）后台纠错/补标点 → 差异替换草稿；设置浮层新增 **AI精修** 开关
- **结果**：✅ v11 上线（`pkg-4`，run-3），AI 精修链路建立

### v12（2026-08-17）— 内联配置（消除灰色困惑）
- **需求**：AI精修开关置灰不可选；"OpenAI 兼容云 ASR"文案让用户分不清
- **改动**：设置浮层内联配置云 ASR 与 DeepSeek Key（填完即用）；引擎选项始终可选，未配置标红提示（不再灰色）
- **结果**：✅ v12 上线（`pkg-6`，run-5）

### v13（2026-08-17）— 实时听写
- **需求**：录音后识别改为实时识别；配置块填完无法更改
- **改动**：录音模式升级为**实时听写**（VAD 断句、说一句识别一句）；云 ASR 与 DeepSeek 配置块始终显示、始终可编辑
- **结果**：✅ v13 上线（`pkg-7`，run-6）

### v14（2026-08-17）— API 配置折叠
- **需求**：API 配置一直显示在浮窗不美观，要折叠按钮
- **改动**：API 配置收进折叠按钮（状态点实时显示云ASR/精修就绪），点开填写、Key 填齐自动收起，浮窗保持简洁
- **结果**：✅ v14 上线（`pkg-8`，run-7）

### v15（2026-08-17）— 持久化
- **需求**：填写的 API 关浮窗后消失，需要长存
- **改动**：设置与 API Key 持久化到浏览器 localStorage（key: `dsh.voice.prefs.v1`）：关闭浮窗/刷新/重启 DSH 均保留，除非用户主动修改；提供「清除保存的 Key」入口
- **结果**：✅ v15 上线（`pkg-9`，run-8）

### v16（2026-08-17）— 上下文精修
- **需求**：开启精修后准确性仍不够——大模型应结合语境规正同音字/口音/不确定项，需核实并改进 prompt
- **改动**：AI 精修携带前后文上下文（`transcribe_chat` 接收 `{text, before, after}` 信封）；DeepSeek 结合语境规正同音字/口音错误；精修 prompt 强化"语义规正"指令（同音字/口音/标点，保持原意与口语风格）
- **配套**：AGENTS.md 工作区规则建立（规则一产物落盘 / 规则二版本留痕）
- **结果**：✅ v16 上线（`pkg-10`，后因 DSH 重启重建为 `pkg-1`）

### v17（2026-08-19）— 可管理版（网络重试 / 失败可视化 / 模型管理 / 精修 Prompt 可编辑）
- **需求**（5 项反馈）：① 默认引擎网络断连导致语音无法加载；② 本地模型点击不说话无响应；③ 模型档位是否有确切产品规格；④ 本地模型无下载/存储位置展示；⑤ AI 精修 mode/prompt 不可知不可改
- **改动**：
  1. 网络断连**自动重试**（stream 模式最多 3 次退避，提示"网络中断，正在重连…"）
  2. **启动即时反馈**（"正在启动麦克风…"）+ **识别失败可视化**（失败红字显示原因，不再静默）
  3. 模型档位给出确切规格表（参数/体积/说明，如 tiny/base/small/medium/large-v3）
  4. **本地模型管理**：Host 新增 `listModels`/`downloadModel` Remote；设置浮层"本地模型"折叠区逐行显示状态/体积/已下载✓/下载按钮/存储路径（`.hf`）；下载走 hf-mirror
  5. **精修 Prompt 可编辑**：设置浮层 textarea，持久化到 prefs（留空=默认）
- **结果**：✅ v17 冒烟通过（5 个 Remote 就位、入口在、client.js 200）；此后进入静态部署版迭代

### v18（2026-08-21）— 模型语义标注 + 云 ASR 预设
- **改动**：模型下拉消除误导（自动模式下模型/质量显示"随引擎自动"语义）；云 ASR 配置新增**预设服务商**（OpenAI / Groq / 硅基流动 / 智谱等），选中自动填入 Base URL 并匹配推荐模型（OpenAI→whisper-1，Groq→whisper-large-v3-turbo），只需粘贴 Key
- **结果**：✅ v18 冒烟通过

### v19（2026-08-21）— 豆包后端 + 引擎联动禁用
- **改动**：豆包（火山引擎）作为**独立引擎**接入——专有 v3 协议（`openspeech.bytedance.com/api/v3/auc`，AppID + Access Token 鉴权，非 OpenAI 兼容），`transcribe.py` 新增 `transcribe_volc` 后端；**引擎联动禁用**：自动→模型/质量禁用；本地→模型可调、质量可调；云→模型可调、质量禁用（标注"仅本地生效"）
- **结果**：✅ v19 冒烟通过

### v20（2026-08-21）— UI 精简（去括号标注）
- **改动**：去掉识别模型/质量选项后的括号标注（如"自动不可调"），界面更干净
- **结果**：✅ v20 冒烟通过

### v21（2026-08-21）— 错误格式化 + 引擎分组 + auto 语义
- **需求**：本地识别失败提示为 `识别失败: [object Object]`；auto 与引擎/模型选项关系混淆；豆包与云 ASR 并列不合逻辑
- **改动**：**错误格式化修复**（`fmtErr`，结构化错误不再显示 [object Object]，红字给出具体原因）；auto 模式下模型/质量显示**「随引擎自动」**（禁用灰显，语义明确）；云 ASR 分组为「云 ASR〔OpenAI 兼容｜豆包〕」
- **结果**：✅ v21 冒烟通过（client.js 53KB）

### v22（2026-08-21）— 引擎三项化 + 豆包并入预设
- **改动**：引擎选择只保留**自动 / 本地 / 云 ASR** 三项；豆包从引擎选择中移除，改为云 ASR 配置区"预设服务商"里的并列选项（与 OpenAI/Groq/智谱/硅基流动并列），选中后切换为 AppID/Token/Cluster 表单；旧配置自动迁移
- **结果**：✅ v22 冒烟通过（client.js 54KB）

### v23（2026-08-21）— 核心修复：Typert 参数描述符（识别/精修真正打通）
- **问题**：本地识别红字 `typert gateway: voice/transcribe: args fields do not match the descriptor: unexpected "wavBase64", "lang", ...`
- **根因**：Typert 网关 SRC 回退按 Host 方法**源码参数名**生成 wire 字段（`transcribe(args)` 只有 `args` 一个字段），客户端却把业务对象直接展开成 payload → 全部字段 unexpected → 网关拒绝
- **连锁影响**：`transcribe`/`polish`/`downloadModel` 全部被网关拒绝——**本地识别从未真正工作、AI 精修从未真正执行**（错误被 `.catch` 静默吞掉）
- **修复**：客户端带参调用统一包 `{ args: {...} }` 与 wire 字段对齐；精修失败不再静默，红字显示「精修失败: <原因>」
- **结果**：✅ v23 部署验证通过，识别与精修链路自此真正可跑

### v24（2026-08-21）— 延迟合并精修（伪句号"掩码"修复）
- **问题**：停顿被识别为句号；逐块立即精修时后续句子尚未说出（after 为空），LLM 无从判断语流是否继续，只能确认句号——"掩码"效应
- **修复**：
  1. **延迟精修**：chunk 上屏后延迟 2000ms 才调 DeepSeek，等后续语流到达
  2. **合并窗口**：期间相邻块（间隔 ≤2 个分隔符，窗口 ≤240 字）自动合并，flush 时从最新草稿重建合并文本一次精修，LLM 看到完整语流，自然把"…。但是…"改回"…，但是…"
  3. **停止录音立即落定**：teardown/stop 时 flush 挂起窗口；卸载时清 timer
  4. **默认精修 prompt 强化**：明确"句号不一定是真实句子边界；结尾接连接词时允许句号改逗号"
- **注**：合并窗口/延迟仅作用于 AI 精修环节，识别与上屏仍实时
- **结果**：✅ v24 部署冒烟通过

### v25（2026-08-21）— 设置弹窗滚动修复 + 产品说明文档
- **问题**：设置弹窗内容超高时顶部选项被视口裁掉且无法滑动
- **修复**：`max-height: calc(100vh - 48px)` + `overflow-y: auto` + 细滚动条样式 + 滚动穿透防护（`overscroll-behavior: contain`）
- **配套**：新增《语音输入-设置界面产品说明.md》（工作区根目录）：识别引擎选择、服务商预设、API 配置的完整设计规格 + 可复用文案/占位符/错误态速查表
- **结果**：✅ v25 部署冒烟通过

### v26（2026-08-22）— 常驻可见滚动条
- **问题**：v25 的滚动条为 `overflow-y: auto` + 淡色滑块（6px、18% 透明度），系统开启「自动隐藏滚动条」或浅色背景下几乎不可见，用户感知为"没有滑块"
- **修复**：`overflow-y: scroll`（常驻显示滚动条槽，不依赖内容溢出判断）+ 8px 宽、32% 透明度滑块 + 浅灰轨道 + hover 加深，并加 Firefox 的 `scrollbar-width/scrollbar-color` 兼容
- **结果**：✅ v26 部署冒烟通过

### v27（2026-08-22）— 本地识别质量根治（常驻 worker / VAD 修正 / 会话代际）
- **问题**（真实使用反馈）：识别速度极慢；频繁 `no speech recognized`；关闭后再打开跳出旧话、删除后重复上屏
- **修复**：
  1. **识别速度**：新增常驻 worker（Host 侧 `LocalWorker` + `transcribe.py --serve`，JSON 行协议）——faster-whisper 模型**只加载一次**，此后每块识别 ~0.3-2s；此前每块都重新 spawn Python 并加载模型（3-10s/句）。worker 崩溃/超时自动回退一次性调用
  2. **频繁 no speech**：本地识别 `vad_filter=False`（浏览器端 VAD 已切块，faster-whisper 内部 Silero VAD 会丢弃短块）+ `condition_on_previous_text=False`（短块独立解码）；浏览器端 VAD 校准防污染（开麦瞬间有声音则重置校准，基线只取纯静音）+ 触发时并入 ~200ms pre-roll 缓冲（防切掉字头）
  3. **旧话跳出/重复上屏**：引入**会话代际**（sessionRef）——startDict/startStream 递增代际，停止时再递增，所有在途/排队识别回调与挂起的 AI 精修窗口一律作废；stopDict 不再等待识别链、teardownStream/stopDict 清空 polish timer 与 pending 窗口
- **行为变更**：**停止录音即静默**——不再回补停止瞬间的最后半句（v24 的"停止立即落定"取消）；自动发送改为停止时立即提交当前草稿
- **结果**：✅ v27 部署验证通过（两个并行会话合并，工作区与部署目录为最终合并版）

### v28（2026-08-22）— 设置弹窗说明精简
- **改动**：设置弹窗底部提示区删除三条长说明（云 ASR 分类、AI精修原理、配置/Key 保存位置），仅保留「自动 = …随引擎自动」一句（句末 `；` 改 `。`）与「清除保存的 Key」按钮
- **结果**：✅ 服务端验证生效

### v29（2026-08-22）— 本地引擎预热（选引擎即冷启动）
- **目标**：把本地识别冷启动（模型加载 5-10s）挪到说话之前
- **改动**：① Host 新增 Remote `warm()` + worker 协议 `op:"load"`（只加载模型不转写，缓存命中 0ms）；② 客户端「选择本地引擎 / 切换本地模型」的瞬间即自动预热；③ 首次按下录音时若尚未预热，先显示「正在启动本地识别引擎（首次约 5-10 秒）…」提示并等待预热完成再开麦（预热失败则降级照常开始，首句由 worker 自行加载）；④ 状态提示改为非错误时也可见（灰色 info 样式）
- **实测**：预热 6.5s（一次性）→ 预热后每块转写 ~0.9s
- **注**：本版本由并行会话实施（2026-08-23 检出时已部署，README 已记录）

### v30（2026-08-22/23）— 预热不阻塞开麦
- **改动**：本地引擎首次录音不再等预热完成——立即进入听写，预热在后台并行（兜底逻辑：预热本应在「选本地引擎」那一刻已完成）；预热期间仅短暂显示「本地引擎预热中…（首句可能较慢）」轻提示，不打扰输入
- **注**：本版本由并行会话实施（代码注释 v30；README 条目由 v31 会话补记）

### v31（2026-08-23）— 连续说话超时强制切块（文字持续涌现）
- **需求**：连续说话不停顿时 VAD 静音门控（700ms）永不触发，文字憋到停顿/停止才上屏——用户感知"说了很久才有文字"
- **改动**（`lib/client.js` VAD 段）：静音门控 `SILENCE_END_MS = 700` **维持不变**（有停顿即提前切块），新增 `MAX_SPEECH_MS = 2500`——连续说话 ≥2.5s 即使无静音也强制 `flush()` 切块识别上屏；flush 后 `speechActive` 保持 true，后续音频继续累积不丢声
- **精修配合**：切碎的半句由 AI 精修合并窗口（v24，间隔 ≤2 分隔符合并 + 2s 延迟）自动拼回修正——"人一边继续说、字一边涌现，AI 一边改写旧字"
- **验证**：`node --check` 语法 ✓ → 快照/部署 hash 一致 ✓ → 运行实例 client.js HTTP 200，markers（`MAX_SPEECH_MS`/`2500`/`SILENCE_END_MS = 700`）在位 ✓
- **待实测**：重启 dsh 后连续说话 ≥2.5s 应有文字持续涌现；`--port` 冒烟受当前 dsh 版本端口配置限制（见 README 排障记录）

### v32（2026-08-23）— 聆听中过程提示（并行会话实施）
- **问题**：录音开始后长时间无文字上屏时完全静默（红圈亮着、说了话却没反应），4 条静默路径：VAD 未触发 / 短语音丢弃 / whisper no-speech / 空结果
- **改动**：录音 ≥3s 仍无上屏 → 状态区灰色 info「聆听中…未识别到语音」每 5s 刷新秒数；有内容上屏即消失；`statusRef` 镜像守卫不覆盖预热/重连/错误状态；计时器挂会话代际
- **注**：本版本由并行会话实施（README 已记录）

### v33（2026-08-23）— 停止录音时落定精修窗口
- **问题**（用户实测反馈）：本地/云引擎（dict 路径）下「说完即停」时，最后一块文字永远不被精修——`stopDict`/`teardownStream` 停止时清空丢弃挂起的精修窗口（v27"停止即静默"的副作用）；而 stream 路径（自动引擎）因持续聆听不受影响——用户感知"本地引擎精修不生效"
- **验证过程**：页面可见诊断确认链路本身全通（自定义 prompt「末尾追加【已精修】」在 stream 路径出现标记 ×3）→ 静态走查确认 dict 路径调用链完整 → 定位到停止清窗逻辑
- **改动**：`stopDict`/`teardownStream` 停止时不再清空，改为立即 `flushPolish()` 落定（识别仍 v27 静默；精修仅改写已有文字不新增；flushPolish 区间校验保护用户编辑）；依赖数组补 `flushPolish`
- **验证**：`node --check` ✓ → 快照/部署同步 ✓ → 3080 实例服务 HTTP 200（69479 字节）
- **待实测**：本地引擎说完即停，2~4 秒后最后一句应出现精修改写（用「末尾追加【已精修】」prompt 验证）

### v57（2026-XX）— 提示自动消失 / 无语音自动停止 / 预热提示升级 / 环境检测
- **需求**（用户反馈四项）：①「已识别」不自动消失；②不说话时「正在识别中/聆听中」无限挂起；③豆包浏览器无法使用；④本地 ASR 启动等待提示不足
- **改动**（`voice-input-plugin/client.js`，纯前端）：
  1. **瞬时提示自动消失**：新增 `flashStatus(msg, ms)`——成功/中性提示（已识别/未识别到语音/已停止/未检测到语音等）默认 **3s** 自动消失；错误提示保留；任何新状态取消旧定时器
  2. **无语音自动停止**：引擎就绪后连续 **15s** 无语音 → 自动停止（「未检测到语音，已自动停止」3s 消失）。预热期间不计时；VAD/响度活动与文字上屏均重置计时（`startDictation` 新增 `onSpeech`、`startBatchCapture` 新增 `onActivity`）；整段模式无语音自动停止且全程无声音时跳过转写直接结束
  3. **预热提示升级（A+B）**：「本地引擎预热中…（已 N 秒，首次约 5-10 秒）」每 5s 刷新；预热完成后短暂显示「引擎就绪」1.5s；预热上限 **30s**，超时提示「引擎启动超时，仍可继续使用」后转入正常聆听计时；预热期间不启动「聆听中」提示
  4. **环境检测（豆包浏览器排查）**：设置面板新增「环境检测」折叠区（安全上下文 / mediaDevices / AudioContext / SpeechRecognition 四项诊断 + 缺失原因与处理建议）；禁用麦克风按钮 tooltip 列出具体缺失项；非安全上下文报错文案细化
- **补充**：整段模式录音中补上缺失的「聆听中…未识别到语音」提示（v36 移除后一直空缺）
- **验证**：`node --check` 语法 ✓；分发副本 hash 一致 ✓
- **待部署**：工作区 + `voice-input-dist` 已同步；部署到 profiles 需用户批准
- **待实测**：豆包浏览器控制台报错（配合环境检测定位）；无语音 15s 自动停止；预热秒数提示

### v58（2026-XX）— 整段尾裁 / 中英切换 / 浏览器适配 / 预热如实提示
- **需求**（用户反馈）：①整段模式 + FunASR 出现大量重复字（如「位置」→ 七个「位」）；②精修 API/prompt「消失」；③新增中英切换（清除 Key 同排右侧）；④小众浏览器主动适配 ASR；⑤一次识别耗时 50s
- **排查结论**（实测）：
  - 重复字：合成语音（含 20s 尾静音/句间停顿/底噪/噪声尾）FunASR **均无重复字**且识别极快（0.2-0.4s，VAD 自动跳过静音）；无法复现用户场景 → 采用缓解措施 + 关联精修兜底
  - 精修消失：部署版 v56 与工作区 v57 代码中「AI精修（DeepSeek）配置」区**完整存在**（逐行核对）→ 最可能原因为**换浏览器导致 localStorage 隔离**（Key/Prompt/设置均存于当前浏览器），待用户确认
  - 50s 元凶：**FunASR 模型冷加载实测 38-39.8s**（三轮一致）+ 首次音频处理 ≈ 50s；通常被「选引擎即后台预热」掩盖，dsh 重启后立刻使用或切换引擎后（worker 销毁重建）会遇到
- **改动**：
  1. `client.js` **整段尾裁**：`startBatchCapture` 停止时按块 RMS（阈值 0.008，保留 ≥2 块 ~170ms）裁剪尾部静音（与听写模式 flush 同款），防长静音尾部幻觉重复字并缩短识别耗时；「正在识别…」提示带音频时长（如「音频 35s，已用 8s」）
  2. `transcribe.py` **FunASR merge_vad**：`am.generate(..., merge_vad=True, merge_length_silence=300)` 合并 VAD 相邻短段消除段边界重复（实测长尾静音场景输出正确）
  3. `client.js` **精修配置说明**：AI精修区新增「Key 与 Prompt 仅保存在当前浏览器本地（localStorage）；换浏览器或清除浏览器数据后需重新填写」
  4. `client.js` **中英切换**：设置底部一行（清除 Key 左侧 / 中英切换右侧），点击在中文↔English 间切换（联动 prefs.lang，浏览器实时识别按 STREAM_LANG_MAP 跟随；FunASR 固定中文禁用）
  5. `client.js` **浏览器适配**：新增 `detectSpeechRecognition()`（webkit/moz/ms/o 前缀 + 大小写兜底扫描），streamSupported/SR 实例化/环境检测均改用；环境检测显示实际探测到的 API 名；stream 失败（网络重试耗尽/service-not-allowed）提示「建议 ⚙ 改用本地/云端引擎」（按用户选择**不自动切换**）
  6. `client.js` **预热如实提示**：文案按引擎显示预期（FunASR「首次约 30-40 秒」/ whisper「约 5-10 秒」），预热上限 30s→60s（30s 会在正常加载中途误报「超时」）
  7. `client.js` **弹窗自适应高度**（用户反馈：设置面板超出屏幕、顶部内容无法查看、滚动条不可用）：原 `max-height: calc(100vh - 48px)` 按「输入栏紧贴视口底部」估算，实际弹窗底边在锚点上方 10px，顶部会伸出屏幕被裁掉且无法滚动到——改为打开时测量锚点（offsetParent）顶部位置动态设置最大高度（= 锚点顶部 - 14px，下限 180px），窗口 resize 时重测；CSS 值保留作兜底
- **验证**：`node --check` ✓；`ast.parse(transcribe.py)` ✓；merge_vad 长尾静音实测 '位置。' ✓；分发副本 hash 一致 ✓
- **待部署**：工作区 + `voice-input-dist` 已同步；部署到 profiles 需用户批准
- **待实测**：豆包等小众浏览器环境检测结果与 stream 失败提示；用户场景下 FunASR 重复字是否缓解；中英切换

### v59（2026-08-27）— 跨浏览器设置持久化（已部署）
- **需求**：精修 Key 确认为浏览器 localStorage 隔离所致（Chrome 有、豆包无）——用户要求「换什么浏览器都存在」
- **方案**：设置与 Key 的存储从浏览器 localStorage 升级为 **Host 侧 `.voice-prefs.json`**（工作区根，fs 服务原子读写，任何浏览器共享同一份）
- **改动**：
  - `host.js`：新增 Remote `getPrefs`（读文件，不存在返回空对象）/ `setPrefs`（原子写 JSON，`fs.resolve + writeText`）；按既有 decorator 模式注册
  - `client.js`：`voiceRemote` 增加 `getPrefs`/`setPrefs`；`prefs.set` 后 400ms 防抖写回 Host（PERSISTED_KEYS 子集）；启动时拉取服务端设置——**服务端优先**合并进本地并写回 localStorage（本地缓存兜底），服务端无数据时自动迁移当前 localStorage 设置（首次使用）；精修配置区提示文案改为「设置与 Key 保存在本机服务端，换浏览器/清除浏览器数据不丢失」
  - 失败降级：Host 不可用/文件读写失败时静默回退 localStorage-only，不影响功能
- **隐私提示**：Key 明文存于本机工作区 `.voice-prefs.json`（与浏览器 localStorage 暴露面相当；README 已注明；换机器不迁移）
- **部署**：用户批准 ✅——先备份部署目录（`voice-input-plugin\.deploy-backup-2026-08-27\`：index.js.v56 / client.js.v56 / cordis.patch.yml.v56），再复制 `host.js → lib\index.js`、`client.js → lib\client.js`，hash 一致 ✓；**需重启 dsh 生效**（Host 代码变更）
- **待实测**：重启后在 Chrome 改设置 → 豆包浏览器打开应看到相同设置与 Key

### v60（2026-08-27）— 界面中英文切换（「中 | EN」分段开关）
- **需求**：用户澄清「中英切换」= **整个界面切英文版**（面向英语母语者），非识别语言；控件改为切分两段的「中 | EN」，选中侧绿色
- **改动**（`client.js`，纯前端）：
  1. 新增界面语言偏好 `uiLang`（'zh'|'en'，默认 zh）——入 PERSISTED_KEYS（随 v59 跨浏览器同步）；**与识别语言 `lang` 完全独立**
  2. 新增 `I18N` 中英文对照字典（~80 键）+ `t(key, vars)` 助手（函数提升，事件/渲染时读取当前 `uiLang`）
  3. **全界面文案双语化**：状态提示（已识别/未识别到语音/聆听中/预热中/正在识别/麦克风错误/网络错误等 ~30 条）、按钮悬停提示（麦克风/齿轮 ~10 条）、设置面板全部（字段/引擎/语言/质量/开关/API 配置/模型管理/引擎说明/环境检测/清除 Key ~40 处）
  4. **「中 | EN」分段开关**：设置底部行右侧，两段按钮，选中侧绿色高亮（`--dsw-alias-state-success-primary`），悬停说明「界面语言，不影响识别语言」
  5. 状态清理适配：`stopWarmHint`/`stopIdleHint` 的中文前缀判断加英文前缀（'Warming up local engine' / 'Listening'），英文界面下预热/聆听提示可正常清除
  6. **v60 补充修复（用户反馈）**：① 引擎加载期误报「聆听中…未识别到语音」——根因：检测到说话（VAD/响度）只重置无语音计时、未清除聆听提示，首句在引擎冷加载 >3s 时被误判为"未识别到语音"；修复：`onSpeech`/`onActivity` 检测到声音即 `stopIdleHint()` 清除聆听提示（用户正在说时绝不显示"未识别到语音"）。② 「中 | EN」分段开关尺寸缩小（高 24→18、padding→6px、字号→10、圆角→9）
  7. **v60 移除「环境检测」（用户要求）**：删除设置面板「环境检测」折叠区（v57 引入的浏览器能力诊断）、相关 `diagOpen` 状态/`diag*` 计算/折叠按钮，及 I18N 中 `set.diag*` 键（zh/en 各 15 个）；禁用按钮 tooltip 不再引用「环境检测」
- **验证**：`node --check` ✓；遗留硬编码中文扫描（仅剩字典条目）✓；`diag`/`环境检测` 引用扫描零残留 ✓
- **部署**：已复制 `client.js → lib\client.js`（含移除环境检测 + 开关缩小，用户授权 ✅），**需重启 dsh 生效**；备份仍在 `.deploy-backup-2026-08-27\`
- **待实测**：切 EN 后全界面英文、切回中文恢复；英文界面下录音/识别/提示正常

### v61（2026-08-27）— 整段识别中 UI 精简 + 三项根治
- **需求1**：整段模式点击停止进入识别后，页面出现 24 段跳动波形 + 冗长「正在识别…（音频 Xs，已用 Ys）」文案，把输入栏拉宽延伸、不美观
- **改动1**（`client.js`）：
  1. **识别中不再渲染波形图**——波形仅在录音中显示（实时红色短波形 `waveLevels`，`live:true`）；停止进入识别后隐藏波形（`waveFinal` 不再用于渲染），输入栏恢复干净（只留状态文案 + 🎤⚙）
  2. **识别状态精简**（用户选 B 方案）——始终「正在识别…」，仅当已用时间 >5s 追加「（已用 Ns）」（`status.recognizing` / `status.recognizingElapsed`，删除原 `recognizingAudio`/`recognizingAudioSec` 键）
- **需求2（用户反馈）**：① 开始录音后首句没录进去，之后反复几次又把前几次结果重复添加；② 换一个对话后首次说话仍要加载很久
- **根因（代码级）**：
  - 首句丢失：VAD 校准期（前 300ms）检测到开口就无限重置并**丢弃音频**（v26 引入），「开麦即开口」时首句整段被丢
  - 重复/错位：`commitChunk` 用绝对偏移 `insPointRef`，AI 精修（默认开）异步改草稿长度后偏移失效；渲染重置清空 `lastCommittedRef` 却不重定位 `insPointRef` → 后续块插到过期坐标，已上屏内容重复堆积
  - 换对话重新加载：`resetWorker` 效果依赖 `[p.engine, p.model]`，**每次对话挂载都执行**，销毁已加载模型（FunASR 冷加载 38s）
- **改动2**（`client.js`）：
  3. **Fix A 首句**：校准期不再丢弃——静音块进滚动 preBuf；开口（rms>0.03）立即结束校准用默认基线 0.01，fallthrough 由 VAD 直接收入（开口块不重复进 preBuf）
  4. **Fix B 重复/错位**：`commitChunk` 优先按上一块文本 `lastIndexOf` 在最新草稿重定位插入点（精修/外部改动后自愈），找不到锚点回退 `insPointRef`（外部改动时已锚定到草稿末尾 → 追加）；渲染重置不再清空 `lastChunkRef`（保留锚点），并把 `insPointRef` 设为草稿末尾
  5. **Fix C 换对话重载**：`resetWorker` 效果加 prev ref 守卫——仅引擎/模型真正变化时销毁 worker（跳过首次挂载与重渲染）
- **验证**：`node --check` ✓；分发副本 hash 一致 ✓
- **部署**：已复制 `client.js → lib\client.js`，**需重启 dsh 生效**

---

## 版本事实核查备注

- **v9–v16 断档补全**：本段原无任何文档记录，2026-08-23 从 DSH 历史会话档案（`C:\Users\catsk\.dsh\sessions\--D-Codex-dsh~8BED~97F3~8F93~5165--\`，zstd 解压后检索 `cordis_define` 包名与迭代总结）逐条还原，包名/变更点/日期均来自原始会话记录
- **v26/v27 编号碰撞**：2026-08-22 凌晨两个会话并行工作——一个做滚动条修复（先记为 v26），另一个做本地识别修复（原记 v26，发现 README 已到 v26 后更正为 v27）；v28 由第三个会话完成，最终顺序 v26（滚动条）→ v27（识别根治）→ v28（文案精简）
- **v29/v30/v31 并行会话**：v29（本地引擎预热）与 v30（预热不阻塞开麦）由并行会话实施，v31（超时强制切块）由本会话实施；检出时 v29 已部署，v30 仅有代码注释——版本号以"先到先得"顺序编排，README/CHANGELOG 由 v31 会话统一补记
- **v23 是分水岭**：此前本地识别与 AI 精修从未真正生效（Typert 网关拒绝 + 错误被静默吞掉）；v23 之后功能才真实可测
