# 语音输入插件 · Python 依赖（setup.ps1 / setup.sh 安装）
# 本地 ASR（faster-whisper，离线）与云端后端（httpx）
faster-whisper>=1.2.1
httpx>=0.27

# FunASR 中文识别（ONNX 路线，无 torch 依赖）
funasr-onnx>=0.2.0
funasr-onnx-automodel>=0.1.0
onnxruntime>=1.17
